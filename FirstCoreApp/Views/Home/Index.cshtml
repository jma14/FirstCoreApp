@model IEnumerable<FirstCoreApp.Models.Video>
@{ 
    ViewData["Title"] = "Videos";
}
        <table>
        @foreach (var item in Model)
        {
            <tr>
                <td>id = @item.Id</td>
                <td>title = @item.Title</td>
            </tr>
        }
        </table>